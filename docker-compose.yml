version: '3.8'

services:
  parking-lease:
    build: .
    container_name: parking-lease-system
    ports:
      - "3001:3001"
    volumes:
      - parking-data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped

volumes:
  parking-data:
